import React from 'react';
import '../scss/main.scss'
import Header from './Header';

const Home = () => {
    return (
        <section id='home'>
            <Header/>
        </section>
    );
}

export default Home;