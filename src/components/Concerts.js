import React from 'react';
import '../scss/main.scss';
import Header from "./Header";

const Concerts = () => {
    return (
        <section id='concerts'>
            <Header/>

        </section>
    );
}

export default Concerts;